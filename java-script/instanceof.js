// result = variable instanceof constructor
console.log(person instanceof Object);  //Output: is the variable person an Object? 
console.log(colors instanceof Array);    //Output: is the variable colors an Array? 
console.log(pattern instanceof RegExp);   //Output: is the variable pattern a RegExp?